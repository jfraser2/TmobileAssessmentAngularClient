import { ValidationErrorDetail } from './validation-error-detail';

describe('ValidationErrorDetail', () => {
  it('should create an instance', () => {
    expect(new ValidationErrorDetail()).toBeTruthy();
  });
});
